
ktor:
  application:
    modules:
      - org.burgas.ApplicationKt.module
  deployment:
    port: 9000
  postgres:
    driver: "org.postgresql.Driver"
    url: "jdbc:postgresql://localhost:5000/rental_service_db"
    username: "postgres"
    password: "postgres"
  redis:
    host: "localhost"